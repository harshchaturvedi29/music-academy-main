/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['images.unsplash.com', 'res.cloudinary.com','encrypted-tbn0.gstatic.com','m.media-amazon.com',"iansportalimages.s3.amazonaws.com"]
    }
};

export default nextConfig;
